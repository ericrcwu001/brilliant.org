# Scorecard: Markov in the Wild  (lesson-markov-chains-10)
| # | gate | owner | status | evidence |
|---|------|-------|--------|----------|
| 1 | Source fidelity | Dept 1 | ✅ | All graded numbers are reused L5/L6/L8 engine-verified values (`interleave-A-vs-B` 1/4 & 3/7; `mastery-challenge` (1/4,1/2,3/4); `which-tool-bet` Ehrenfest (1/4,1/2,1/4)); tool-selection rubric is web/pedagogical synthesis. |
| 2 | Math correctness | Dept 3 Verify | ✅ | engine reproduces 1/4,1/2,3/4 · 3/7 · 3,4,3 · 1/4,1/2,1/4; factcheck + goldens; validate green. |
| 3 | Learning science / efficiency | Dept 1 | ✅ | capstone Bet→Explore→Model→Prove; one objective/beat; symbols are prior-grounded recall. |
| 4 | Misconceptions | Dept 1 | ✅ | `which-tool-bet` byOption (opposite-of-stuck / Pⁿ↛π period-2), one `correct:true`; tool-mismatch refutations at `discriminate`/`mastery-challenge`. |
| 5 | Interactivity | Dept 2 | ✅ | every beat genuine DM or allowed scaffolding: retrievalGrid×2, prediction, chainBoard classify-chips (`classify-one`), masteryChallenge×2; heroes `walk-recall`/`explore-mixed`; primer/recap exempt. `Check` disabled until input (interaction test: `classify-one` chips present, no auto-pass). |
| 6 | Assessment / mastery / continuity | Dept 1 | ✅ | retrieval opener (`recall-pick-the-tool`); early win (`classify-one`); REQUIRED `mastery-challenge` before `recap`; two `masteryChallenge` + `interleave-A-vs-B` = strong interleaving; continuity = recall/synthesis. **Required-flag HARMONIZED:** `which-tool-bet`/`walk-recall`/`explore-mixed`/`recap` now `required:true` (only track:A `name-the-rubric` false). |
| 7 | Accessibility & mobile | Dept 2 | ✅ | ≥44px + `aria-live`; both heroes `reducedMotionFinalFrame` + `structuralReadout`. |
| 8 | Technical implementation | Dept 3 | ✅ | validate + tsc + vitest 1111/1111 + eslint green; all-reuse, surgical. |
| 9 | Inclusivity gate | Dept 3 Verify | ✅ | inclusivity + mastery pass; primer `name-the-rubric` track:A; byOption `which-tool-bet`; interviewNote `discriminate`; walkBoard hero block. |

**Overall:** READY — 9/9 gates green; required-flag harmonized to `required:true` for the track:both ungraded beats. **N3 (`classify-one`) ACCEPTED:** classifying each state R/T/A is exactly how you determine absorbing-vs-ergodic, the "ergodic" headline is engine-verified, and the mechanic fits the question (no change needed).
