# Scorecard: The Markov Property  (lesson-markov-chains-1)
| # | gate | owner | status | evidence |
|---|------|-------|--------|----------|
| 1 | Source fidelity | Dept 1 | ✅ | `read-the-edge` headline `7/10` = matrix[1][1] (Rainy row), Math.SE 3336273; `recall-no-memory`/`markov-or-not` anchored GB p.53 §5.1 ("only the current state matters"); `mastery-augment` accept `["4/5"]` is the brief's **[CONSTRUCTED]** augmentation item, engine-verified. All graded values trace to brief's verified-answers table. |
| 2 | Math correctness | Dept 3 Verify | ✅ | engine reproduces `P(rain\|rain)=7/10` (chainBoard headline == `matrixPower(weather,1)[1][1]`) and `P(rain\|RR)=4/5` (augmented chain golden); `validate-fixtures` green; `lesson-markov-chains-1.factcheck.test.ts` in 1033/1033 vitest. |
| 3 | Learning science / efficiency | Dept 1 | ✅ | Bet (`streak-bet`) → Explore (`read-the-edge`, `step-the-weather`) → Model (`name-memoryless`) → Prove (`markov-or-not`, `mastery-augment`); one objective/beat; concreteness-fading coin→weather→`P(Xₙ₊₁\|Xₙ)`; symbol introduced at `name-markov`/`name-memoryless` only after concrete referents (beats 1–2,4–5). |
| 4 | Misconceptions | Dept 1 | ✅ | All 5 brief misconceptions elicited+refuted: gambler's-fallacy (`streak-bet`,`recall-no-memory`), memoryless≠independent (`name-markov`/`name-memoryless` h2), read-the-column (`read-the-edge` h2), any-past-breaks-Markov (`markov-or-not` h2), 2-day-never-Markov (`mastery-augment`). Both `prediction` byOption notes name+refute the SPECIFIC wrong option. |
| 5 | Interactivity | Dept 2 | ✅ | every beat genuine direct-manipulation (retrievalGrid match ×2, prediction commit ×2, chainBoard tap-edge/step-token ×2, tripletReveal tap-3-lenses, masteryChallenge type); only `name-markov` (primer) + `recap-now` (recap) are the allowed reveal/tap-light scaffolds. No text walls / fake reveals. |
| 6 | Assessment / mastery / continuity | Dept 1 | ✅ | graded `retrievalGrid` opener (`recall-no-memory`, first graded); guaranteed early win = single-cell read (`read-the-edge`), not hardest type; REQUIRED `masteryChallenge` (`mastery-augment`) immediately before `recap-now`; interleave seed (`remembers-vs-forgets` → L4 absorbing / L7 ergodic); continuity-report L1 row = reuse-as-RECALL ("coin has no memory" → "next depends only on now"), not re-teaching. |
| 7 | Accessibility & mobile | Dept 2 | ✅ | reused renderers carry 44px taps + `aria-live` (verified in `ChainBoardBeat`/`TripletRevealBeat`/`PredictionBeat`); hero `step-the-weather` opts in with `hero.reducedMotionFinalFrame:true` + `structuralReadout` mirrored to `sr-only`; diagram nodes/edges are `<button>` with `role="status" aria-live`. |
| 8 | Technical implementation | Dept 3 | ✅ | `tsc -b` ✓ · vitest 1033/1033 ✓ · eslint ✓ · `validate-fixtures` "All fixtures valid."; schema-valid; surgical (every L1 type maps to a real renderer in `beats/index.tsx`, none to `ContinueStub`). |
| 9 | Inclusivity gate | Dept 3 Verify | ✅ | `validate-fixtures` inclusivity gate (lesson-markov-chains-1) + mastery-challenge gate both pass per QA-Lead first-hand run. |

**Overall:** READY — all 9 gates green; no hint leaks, no non-interactive beats, byOption refutations are specific, and both `required:false` beats (`name-markov` track:A, `remembers-vs-forgets` track:B) are correctly track-gated.
