# Scorecard: The Scale Speaks Base-3  (lesson-binary-information-4)

> Verified against `fixtures/lesson-binary-information-4.json`,
> `src/content/lesson-binary-information-4.factcheck.test.ts`, `brief.md`, `interaction-spec.md`,
> `src/engine/binary.ts`, and `scripts/validate-fixtures.ts`.

| # | gate | owner | status | evidence |
|---|------|-------|--------|----------|
| 1 | Source fidelity | Dept 1 | ✅ | Headline 12-coin defective-ball anchored to **GB Ch.2 p.4–5** ("12 balls … heavier OR lighter … 3 measurements … (3ⁿ−3)/2 balls in n measurements"); S9 (Wikipedia/cut-the-knot) corroborates. 9 coins→2 = S5; Bachet {1,3,9,27} = S10 (arXiv/MathWorld); 22 g transfer = same balanced-ternary method. Recall 3³=27 = `lesson-combinatorics-1` counterfeit-coin teaser (dedupe→promote). All cited. |
| 2 | Math correctness | Dept 3 Verify | ✅ | Engine reproduces every accept: `l4-win`=2 (`weighingsForN(9,true)=2`), `l4-apply`=`1,3,9,27`/`27` (`bachetWeights(40)=[1,3,9,27]`), `l4-prove`=3 (`weighingsForN(12,false)=3`). weighing headlines: `l4-explore`/`l4-model` `scale` items=12 unknown → `"3"`; `l4-transfer` `ternary` target=22 weights {1,3,9,27} → `"+1,-1,+1,+1"` (=`balancedTernary(22,[27,9,3,1])`, verified 27−9+3+1=22) — recomputed by `validate-fixtures §3f` + §2e self-check. Factcheck passes. |
| 3 | Learning science / efficiency | Dept 1 | ✅ | Bet (`l4-bet` 12 coins) → Explore (`l4-explore` load pans, read 3-way tilt) → Model (`l4-model` ⌈log₃N⌉ vs ⌈log₂N⌉, unknown-direction slack) → Prove (`l4-prove`). One objective/beat; the alphabet (3 outcomes) is taught via the primer before ⌈log₃⌉. |
| 4 | Misconceptions | Dept 1 | ✅ | 5 wrong models (weighing-is-binary; halving⇒6; split-in-half; weight-per-gram/only-powers-of-2; unknown-direction-impossible-in-3). `l4-bet` byOption refutes "6"/"4"; correct="3". `l4-apply` hint refutes "only add weights" → balanced ternary (place with the object = subtract). |
| 5 | Interactivity | Dept 2 | ✅ | `l4-explore` genuine direct-manipulation (`weighing scale`: drag groups to pans, beam tilts L/level/R, weighings counter). `l4-transfer` `weighing ternary` is an interactive ±weight-placement widget. `WeighingBeat.tsx` renderer confirmed. |
| 6 | Assessment / mastery / continuity | Dept 1 | ⚠️ | Retrieval opener `l4-recall` (`retrievalGrid`, first graded = early win) ✓. Required `masteryChallenge` `l4-prove` (accept `3`) immediately before recap ✓. **Minor deviation (NOT a failure):** the held-out transfer `l4-transfer` (track:'B', required:false, immediately before mastery) is a `weighing/ternary` **interactive beat** with engine-verified headline `balancedTernary(22)`, NOT a graded `answerEntry` like L1–L3/L5/L6. It is a genuine engine-verified interactive transfer on a fresh surface (22 g, same base-3 method), and the required mastery challenge still grades the core method (12 coins→3). Documented as a Manager-arbitrated shape in `interaction-spec.md` (design note). Continuity: combinatorics-1 3³=27 teaser promoted to the real lesson; base-2↔base-3 chapter spine. |
| 7 | Accessibility & mobile | Dept 2 | ✅ | `WeighingBeat.tsx`: `reducedMotion` snaps beam to final tilt (no swing) / static balanced frame for ternary; `aria-live` mirrors tilt + counter + placement equation; pans/weights carry `aria-label`; tilt buttons `aria-pressed`. 44px via CSS. e2e deferred to dev smoke + user-run. |
| 8 | Technical implementation | Dept 3 | ✅ | validate/test/build/lint green; factcheck + goldens pass; `balancedTernary` added in Wave 0 (47 goldens). Surgical reuse + `weighing`. Zero new failures. e2e deferred. |
| 9 | Inclusivity gate | Dept 3 Verify | ✅ | In `GATED` + `MASTERY_LESSONS`; mechanized inclusivity + mastery gates ran and passed. |

**Overall:** READY — GB defective-ball headline, every accept and both weighing headlines engine-reproduced; the one transfer-shape deviation (interactive `weighing/ternary` vs graded `answerEntry`) is engine-verified, correctly placed before a required mastery challenge, and Manager-documented — a minor deviation, not a gate failure.
