# Scorecard: The Update Rule  (lesson-bayes-rule-1)

> Reviewer sign-off (Manager-delegated). Gates + pass conditions per `.cursor/skills/lesson-factory/qa-rubric.md`.
> Beats: `recall-prob-split` → `open-bet` → `name-the-parts` → `explore-update` → `count-the-heads` →
> `compute-posterior` → `framing-flip` → `triangulate-23` → `mastery-challenge` → `recap`.

| # | gate | owner | status | evidence |
|---|------|-------|--------|----------|
| 1 | Source fidelity | Dept 1 | ✅ | `brief.md` problem table cites GB Ch.4 §"Conditional Probability and Bayes' Formula" p.37–38 (boys-girls 1/3 & "meet a boy" 1/2) + web sources for the two-coin → 2/3 (stats.stackexchange q514627; mathproblems.info/prob16s) and two-urn → 2/3. Every headline is an exact rational. |
| 2 | Math correctness | Dept 3 Verify | ✅ | Engine reproduces every value: `explore-update`/`count-the-heads` posterior **2/3** cross-checked live (`validate-fixtures.ts:131-157` → "bayesUpdate posteriors match bayes.ts (4 beats)"); `compute-posterior` 2/3, `framing-flip` 1/3 & 1/2, `mastery-challenge` HH→4/5 all reproduced by `bayes.test.ts` goldens + inline `validate` goldens (`#1,#3,#4,#2`). 55 bayes vitest pass. `bayes.ts` is pure exact-rational (no floats; floats only set cosmetic bar-width %). |
| 3 | Learning science / efficiency | Dept 1 | ✅ | Bet→Explore→Model→Prove: bet `open-bet` → felt explore `explore-update` (bars) + `count-the-heads` (tap-partition) → model `compute-posterior`/`framing-flip` → prove `triangulate-23`/`mastery-challenge`. One objective per beat; JIT primer `name-the-parts` (Track A) names prior/likelihood/posterior before symbols; matches `interaction-spec.md` 1:1. |
| 4 | Misconceptions | Dept 1 | ✅ | `open-bet` (`prediction`) uses per-option `byOption` refutations — "Still 50/50" and "It's certain" each refuted with the brief's Misconception-Specialist copy (`validate` inclusivity asserts every `prediction` carries `byOption`). |
| 5 | Interactivity | Dept 2 | ✅ | `bayesUpdate` is genuine direct-manipulation: drag the prior split → posterior bar recomputes via `bayesPosterior` (`BayesUpdateBeat.tsx:48-51`); `count-the-heads` taps 2-of-3 focal icons (`focalCount = round(2/3·3) = 2`, exact). No text walls / fake reveals. (Its *visual* response depends on the missing stylesheet — tracked under gate 8.) |
| 6 | Assessment / mastery / continuity | Dept 1 | ✅ | Retrieval opener `recall-prob-split` is the first graded beat (`retrievalGrid`, guaranteed early win, not the hardest type); it recalls Penney's/Gambler's "a win-chance is a split, no +1" per the Continuity Report — overlap → recall, no re-teach. Required `mastery-challenge` (HH→4/5) sits immediately before `recap` (`validate` mastery gate ✓). Spaced exact-fraction posteriors (2/3, 4/5) seed L3. |
| 7 | Accessibility & mobile | Dept 2 | ✅ | `role="status" aria-live="polite"` posterior/count mirrors (`BayesUpdateBeat.tsx:121-123, 372-374`); 44px tap targets on the range + icon buttons (`:99, :364`); reduced-motion gates every transition + initializes `interacted` true so Continue is reachable; native range/`<button>` (keyboard) + `aria-pressed`/`aria-label`. Matches the spec's a11y column. |
| 8 | Technical implementation | Dept 3 + Reviewer | ❌ | `validate-fixtures` + vitest + tsc + eslint + vite build green; diff surgical (`AGENTS.md`). **BLOCKER:** the new beat ships with **no stylesheet** — `bayes-bars*/tree*/icons*/seq*` classes exist only in `BayesUpdateBeat.tsx`; `git grep bayes -- src/**/*.css` = none (existing hero beats live in `beats-extended.css`). "Uses design tokens" fails. e2e not run (rubric caveat: Playwright `webServer` uses forbidden `npm run dev`). |
| 9 | Inclusivity gate | Dept 3 Verify | ✅ | `lesson-bayes-rule-1` is in `validate-fixtures.ts` `GATED` (:169) + `MASTERY_LESSONS` (:309); the run prints "inclusivity gate: lesson-bayes-rule-1" and "mastery-challenge gate: lesson-bayes-rule-1" — the gates run for real (not the documented vacuous-pass trap). |

## Issues
- **BLOCKER (gate 8 / design system):** no `.bayes-*` CSS anywhere. The prior/posterior bars have only an inline `width:%` (no height/background/flex), so the signature "watch the posterior swing" renders invisible; the confusion grid degrades to a bare HTML table. Fix is additive + low-risk: add a `.bayes-*` block to `src/styles/surfaces/beats-extended.css` using `--ergo-*`/`--chN`/`--s*`/`--r-*` tokens (mirror the race/walk/ledger blocks). The component hardcodes no colors, so no `.tsx` change is needed.
- **Non-blocking:** brief problem table shows `☐ engine` unchecked though the goldens do reproduce every value (stale checkbox). The `count-the-heads` icon glyph is a hardcoded `🪙` for both hypotheses (distinguished by `aria-label` + CSS tint); the focal-vs-not visual distinction also rides on the missing CSS.

**Overall:** NOT READY — 8/9 gates green; the *only* blocker is the missing `.bayes-*` stylesheet (gate 8 design-tokens). Flip to READY the moment `beats-extended.css` gains the Bayes block; no math/pedagogy/a11y rework required.
