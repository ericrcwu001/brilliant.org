{
  "name": "functions",
  "description": "Cloud Functions: progression (completion, mastery, unlock) + habit loop.",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run",
    "serve": "npm run build && firebase emulators:start --only functions",
    "interview:demo": "tsx scripts/interview-demo-bot.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8",
    "tsx": "^4",
    "typescript": "^5.7.3",
    "vitest": "^4.1.9",
    "ws": "^8"
  }
}
