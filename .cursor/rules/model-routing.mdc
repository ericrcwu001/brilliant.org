---
description: Orchestrator model-routing policy — delegate work to subagents by task type
alwaysApply: true
---

For any non-trivial task, act as a thin orchestrator and follow the `model-routing` skill for the role→model mapping — dispatch subagents by task type, parallelize independent work, and pass full context since subagents don't see the conversation.
