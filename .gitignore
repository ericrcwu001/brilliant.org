# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright (beat-audit harness)
test-results
playwright-report
.playwright-artifacts*

# Stray default-config snapshots from ad-hoc runs.
# Real VR baselines live in e2e/vr/__screenshots__/ and stay tracked.
*.spec.ts-snapshots/

# Environment files
.env
.env.*
!.env.example

# Firebase service-account keys and credentials
serviceAccount*.json
service-account*.json
*-service-account*.json
firebase-adminsdk*.json
.runtimeconfig.json

# Firestore exports / backups
firestore-export/
firestore-backup/
*.firestore-export

# Firebase CLI / emulator artifacts
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
database-debug.log
pubsub-debug.log
*-debug.log
firebase-export-*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vendored agent skills (firebase/agent-skills). Restored locally from
# skills-lock.json; the skill content itself is not committed to the repo.
.agents/skills/

# Ground-truth reference texts for the lesson factory (copyrighted source
# material — kept local, never committed). Agents read these as the only
# valid source of lesson problems.
references/

# Interview demo bot artifacts (audio, transcripts, reports — not committed).
artifacts/

# Lesson-factory isolated worktrees (per-concept + per-lesson builds)
.lf/
